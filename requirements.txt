# Core dependencies
torch>=2.0.0
transformers>=4.30.0
tiktoken>=0.5.0
langgraph>=0.0.1
tqdm>=4.65.0

# Optional: for better performance with transformers
# accelerate>=0.20.0
# bitsandbytes>=0.39.0  # For quantization support
